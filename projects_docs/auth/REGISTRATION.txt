DATABASE ->
    1.USER TABLE ->
        1. UNIQUE USER NAME -> UNIQUE , AUTO GENERATE
        2. PASSWORD -> HASH VALIDATE PASSWORD
        3. NAME -> 
        4. EMAIL -> UNIQUE 
        5. PHONE NUMBER -> 
        6. ACTIVE -> 0 AND 1
    3. USER ROLE TABLE ->
        1. USER ID
        2. ROLE ID
    2. PERMISSION MODULE TABLE ->
        1.USER ID
        2. MODULE ID
FRONTEND ->
    1. CREATE BY SUPER ADMIN ->
        1. NAME -> 
        2. EMAIL -> UNIQUE 
        3. PHONE NUMBER -> 
        4. ROLE OPTIOANL -> (MULTIPLE ROLE )
    2. PERMISION UPDATE BY SUPER ADMIN ->
        1.UNIQUE USER NAME -> SHOW
        2.NAME -> SHOW
        3.MULTIPLE SELECT PERMISION FOR MODULE -> BOX