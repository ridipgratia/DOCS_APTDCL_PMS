DATABASE ->
    1.FORGOT PASSWORD TABLE ->
        1. USER ID (UNIQUE)
        2. FORGOT KEY -> UNIQUE 
        3. EXPIRE TIME -> TIMESMAPT
        4. USED -> 1 AND 0
FRONTEND ->
    1. FORM 1 ->
        1. EMAIL -> INPUT
    2. FORM 2->
        1. OTP
    2. FORM 2 ->
        1.NEW PASSWORD
        2.CONFIRM PASSWORD
